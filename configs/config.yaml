model:
  name: "your_model_name"
  version: "1.0"

training:
  learning_rate: 0.001
  epochs: 10
  batch_size: 32
  test_size: 0.2
  random_state: 42

data:
  raw_path: "data/raw"
  processed_path: "data/processed"

mlflow:
  experiment_name: "default"
